class Gripper:
    def __init__(self):
        # for joint in self.end_effector_gear_joints:
            #     c = p.createConstraint(self.id,9,self.id,10,jointType=p.JOINT_GEAR,jointAxis=[1, 0, 0],parentFramePosition=[0, 0, 0],childFramePosition=[0, 0, 0])
            #     p.changeConstraint(c, gearRatio=-1, maxForce=50)
        pass
    
    def do(self):
        pass 